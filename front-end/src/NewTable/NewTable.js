import React, { useEffect, useState, Fragment } from "react";

/**
 * Defines the NewTable page.
 * @param 
 *  the date for which the user wants to view reservations.
 * @returns {JSX.Element}
 */

export default function NewTable(){
    return <Fragment>NewTable</Fragment>
}